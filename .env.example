# Runtime environment (e.g., dev, production)
NODE_ENV=

# Database connection URL (format: mysql://user:password@host:port/database)
DATABASE_URL=

# Secrets for JWT
JWT_ACCESS_SECRET=
JWT_REFRESH_SECRET=

# JWT token expiration times (e.g., 1d, 7d, 15m)
JWT_ACCESS_EXPIRES=
JWT_REFRESH_EXPIRES=
